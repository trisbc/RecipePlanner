import { BrowserRouter } from 'react-router-dom';
import Footer from './Footer';

export default {
  title: 'Footer',
};

export const Usage = () => (
  <BrowserRouter>
    <Footer />
  </BrowserRouter>
);
